# === CONFIGURACIÓN DE BASE DE DATOS ===
# URL de conexión a PostgreSQL (Neon DB)
DATABASE_URL=postgresql://usuario:password@host:5432/database_name?sslmode=require

# === CONFIGURACIÓN DE AUTENTICACIÓN ===
# Secreto para JWT (debe ser una cadena larga y segura)
JWT_SECRET=tu_jwt_secreto_super_seguro_aqui
# Tiempo de expiración de tokens JWT
JWT_EXPIRES_IN=7d

# Secreto para NextAuth (debe ser una cadena larga y segura)
NEXTAUTH_SECRET=tu_nextauth_secret_aqui
# URL base de la aplicación
NEXTAUTH_URL=http://localhost:3000

# === CONFIGURACIÓN DE SEGURIDAD ===
# Máximo número de intentos de login fallidos
MAX_LOGIN_ATTEMPTS=5
# Tiempo de bloqueo en minutos después de exceder intentos
LOCKOUT_TIME_MINUTES=15

# === CONFIGURACIÓN DE ARCHIVOS ===
# Tamaño máximo de archivo en bytes (5MB = 5242880)
UPLOAD_MAX_SIZE=5242880
# Tipos de imagen permitidos
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/jpg

# === CONFIGURACIÓN DE ENTORNO ===
# Entorno de ejecución (development, production)
NODE_ENV=development

# === INFORMACIÓN DE LA EMPRESA ===
# Nombre de la óptica
EMPRESA_NOMBRE=Tu Optica Nombre
# Dirección física de la óptica
EMPRESA_DIRECCION=Tu direccion completa aqui
# Teléfono de contacto
EMPRESA_TELEFONO=Tu telefono aqui